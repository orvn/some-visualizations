---
import BaseLayout from '../layouts/BaseLayout.astro';
import { loadPage } from '../lib/content';

const { meta, options } = loadPage('404');
---

<BaseLayout
  title={meta.title ?? undefined}
  description={meta.description}
  noindex={options?.noindex ?? false}
>
  <section class="error-page">
    <div class="error-top">
      <p class="error-eyebrow">Page not found &middot; &alpha; = 0.05</p>
      <h1 class="error-heading">Outside the confidence interval.</h1>
      <p class="error-sub">With 95% confidence, this page does not exist.</p>
      
      <div class="error-ci">
        <p class="ci-label">&mu; &middot; KNOWN PAGES</p>
        <div class="ci-bar">
          <div class="ci-range">
            <span class="ci-dot"></span>
          </div>
          <div class="ci-outlier">
            <span class="ci-404">404</span>
            <span class="ci-dot-red"></span>
            <span class="ci-you">YOU ARE HERE</span>
          </div>
        </div>
        <div class="ci-labels">
          <span>-1.96&sigma;</span>
          <span>+1.96&sigma;</span>
        </div>
      </div>

    </div>

    <div class="error-actions">
      <a href="/" class="error-btn">Go home</a>
    </div>

  </section>
</BaseLayout>

<style>
  .error-page {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: calc(100vh - var(--header-height));
    padding: 3rem var(--container-padding) 2rem;
    text-align: center;
  }

  .error-top {
    margin-bottom: 3rem;
  }

  .error-eyebrow {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.2em;
    color: var(--sienna);
    margin-bottom: 1rem;
  }

  .error-heading {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 400;
    font-size: clamp(3rem, 10vw, 5rem);
    line-height: 0.9;
    color: var(--parchment);
    margin-bottom: 1.5rem;
  }

  .error-sub {
    font-size: var(--text-base);
    color: var(--teak);
    line-height: var(--leading-relaxed);
    max-width: 48ch;
    margin-inline: auto;
  }

  .error-ci {
    width: 100%;
    max-width: 52rem;
    margin-bottom: 3rem;
  }

  .ci-label {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    letter-spacing: 0.15em;
    color: var(--potters-clay);
    margin-bottom: 0.5rem;
  }

  .ci-bar {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .ci-range {
    flex: 1;
    height: 2.25rem;
    background: rgba(144, 184, 120, 0.15);
    border: 0.0625rem solid var(--olivine);
    border-radius: 0.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .ci-dot {
    width: 0.75rem;
    height: 0.75rem;
    background: var(--olivine);
    border-radius: 50%;
    opacity: 0.6;
  }

  .ci-outlier {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    flex-shrink: 0;
  }

  .ci-404 {
    font-family: var(--font-display);
    font-style: italic;
    font-size: var(--text-2xl);
    color: var(--sienna);
    line-height: 1;
  }

  .ci-dot-red {
    width: 0.875rem;
    height: 0.875rem;
    background: var(--sienna);
    border-radius: 50%;
    animation: pulse404 2s ease-in-out infinite;
  }

  .ci-you {
    font-family: var(--font-mono);
    font-size: 0.5625rem;
    letter-spacing: 0.15em;
    color: var(--sienna);
  }

  .ci-labels {
    display: flex;
    justify-content: space-between;
    max-width: calc(100% - 5rem);
    margin-top: 0.375rem;
    font-family: var(--font-mono);
    font-size: 0.625rem;
    color: var(--potters-clay);
  }

  .error-actions {
    display: flex;
    gap: 0.75rem;
  }

  .error-btn {
    padding: 0.625rem 1.5rem;
    background: var(--parchment);
    border: 0.0625rem solid var(--teak);
    border-radius: 0.375rem;
    color: var(--graphite);
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: 500;
    text-decoration: none;
    transition: background-color var(--transition-fast);
  }

  .error-btn:hover {
    background: var(--colonial);
  }

  @keyframes pulse404 {
    0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(240, 120, 88, 0.4); }
    50% { opacity: 0.7; box-shadow: 0 0 0 0.5rem rgba(240, 120, 88, 0); }
  }

  @media (max-width: 30em) {
    .error-heading {
      font-size: 2.5rem;
    }

    .ci-bar {
      gap: 1rem;
    }

    .ci-404 {
      font-size: var(--text-xl);
    }

    .error-sub br {
      display: none;
    }
  }
</style>
